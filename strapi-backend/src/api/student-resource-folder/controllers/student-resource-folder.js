'use strict';

/**
 * student-resource-folder controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::student-resource-folder.student-resource-folder');
